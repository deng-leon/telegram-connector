server.port=9898

# Example Camunda 8 SaaS configuration (do not commit real credentials)
# camunda.client.mode=saas
# camunda.client.auth.client-id=<your-client-id>
# camunda.client.auth.client-secret=<your-client-secret>
# camunda.client.cloud.cluster-id=<your-cluster-id>
# camunda.client.cloud.region=<your-region>

# Optional explicit base URL override for webhook registration
# baseUrl=https://example.com

# Example local secret mapping for development only
# camunda.connector.secret-provider.properties.secrets.TELEGRAM_BOT_TOKEN=<your-bot-token>

# Debug logs
logging.level.io.camunda.connector.runtime=DEBUG
logging.level.io.camunda.connector.telegram=DEBUG
